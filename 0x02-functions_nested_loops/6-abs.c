#include "main.h"
/**
 * _abs - compute abs of a integer
 * @n: is the num we are checking for remainder
 *
 * Return: the abs of the int.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
