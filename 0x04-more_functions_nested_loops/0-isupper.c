#include "main.h"
/**
 * isupper - This program checks a character and states whether it is uppercase
 * @c: The character
 * Return: Always 0
 */

int _isupper(int c);
{

	if (c >= 'A' && c <= 'Z')
		Return(1);
	else
		Return(0);
}

