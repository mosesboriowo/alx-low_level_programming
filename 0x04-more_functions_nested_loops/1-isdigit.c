#include "main.h"
/**
 * isdigit - This code checks a character, stating if it a digit or not
 * @c: The Character
 * Return: Always 0.
 */

int _isdigit(int c);
{
	if (c >= '0' && c <= '9')
		Return(1);
	else
		Return(0);
}
